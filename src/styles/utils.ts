'use client'

import styled from 'styled-components'

export const Container = styled.div`
  margin-top: 4vmin;
  padding-inline: 2rem;
  width: min(1110px, 100% - 1rem * 2);
  margin-inline: auto;
`
